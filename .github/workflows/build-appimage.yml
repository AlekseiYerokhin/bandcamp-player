name: Build AppImage

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-appimage:
    uses: ./.github/workflows/appimage-build.yml
    with:
      create-release: true